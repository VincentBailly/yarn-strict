# yarn strict

Drop-in replacement for `yarn install` which increases strictness by disallowing phantom dependencies.

Usage:

Instead of running

```
yarn
```

or

```
yarn install
```

Simply run

```
npx yarn-strict
```
